import { createStore } from 'vuex'

import * as todos from '@/store/modules/todo.js';

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
      todos
  }
})
